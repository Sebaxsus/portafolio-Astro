---
interface Props {
    class?: string,
    id: string
}

const { class: className, id: id } = Astro.props
---

<section id={id} class={`w-full lg:w-[740px] mx-auto ${className} pb-20`}>
    <slot />
</section>